{% extends "base.html.twig" %}

{% block title %}fhooe/router-skeleton{% endblock %}
{% block main %}
    <h1>fhooe/router-skeleton</h1>
    <p class="lead">A skeleton application for fhooe/router.</p>

    <h2>GET /</h2>
    <p>This is the view for the "GET /" route, also called the root route.</p>
    <p>This simply acts as the index page. Try other routes below:</p>

    <div class="list-group">
        <a href="{{ url_for("/form") }}" class="list-group-item list-group-item-action d-flex gap-3 py-3">
            <i class="bi-input-cursor" style="font-size: 2em"></i>
            <div class="d-flex gap-2 w-100 justify-content-between">
                <div>
                    <h6 class="mb-0">GET /form</h6>
                    <p class="mb-0 opacity-75">A simple PHP-based form that can be submitted via POST method.</p>
                </div>
            </div>
        </a>
        <a href="{{ url_for("/twigform") }}" class="list-group-item list-group-item-action d-flex gap-3 py-3">
            <i class="bi-input-cursor-text" style="font-size: 2em"></i>
            <div class="d-flex gap-2 w-100 justify-content-between">
                <div>
                    <h6 class="mb-0">GET /twigform</h6>
                    <p class="mb-0 opacity-75">A form rendered using the Twig template engine that can be submitted
                        via POST method.</p>
                </div>
            </div>
        </a>
        <a href="{{ url_for("/staticpage") }}" class="list-group-item list-group-item-action d-flex gap-3 py-3">
            <i class="bi-code-square" style="font-size: 2em"></i>
            <div class="d-flex gap-2 w-100 justify-content-between">
                <div>
                    <h6 class="mb-0">GET /staticpage</h6>
                    <p class="mb-0 opacity-75">A static HTML page for displaying content that does not change.</p>
                </div>
            </div>
        </a>
        <a href="{{ url_for("/a/route/that/does/not/exist") }}"
           class="list-group-item list-group-item-action d-flex gap-3 py-3">
            <i class="bi-exclamation-circle" style="font-size: 2em"></i>
            <div class="d-flex gap-2 w-100 justify-content-between">
                <div>
                    <h6 class="mb-0">GET /a/route/that/does/not/exist</h6>
                    <p class="mb-0 opacity-75">Example for an undefined route that will trigger the 404 view.</p>
                </div>
            </div>
        </a>
    </div>
{% endblock main %}
